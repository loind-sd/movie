apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: movie-showtime
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/loind-sd/movie.git'
    targetRevision: main
    path: helm
    helm:
      valueFiles:
        - values-showtime.yaml # ArgoCD sẽ dùng file này cho Showtime
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
