name: Build and Deploy Showtime

# Chỉ chạy quy trình này khi có code được push vào thư mục showtime/ hoặc file cấu hình của nó
on:
  push:
    branches:
      - main
    paths:
      - 'showtime/**'
      - '.github/workflows/build-showtime.yml'

env:
  IMAGE_NAME: loind/showtime-service

jobs:
  build-and-update-manifest:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0 # Quan trọng để có thể push code ngược lại

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Set short git commit SHA
        id: vars
        run: echo "sha_short=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: ./showtime
          file: ./showtime/Dockerfile
          push: true
          tags: ${{ env.IMAGE_NAME }}:${{ steps.vars.outputs.sha_short }},${{ env.IMAGE_NAME }}:latest

      # Bước quan trọng: Cập nhật file values-showtime.yaml cho ArgoCD
      - name: Update values.yaml
        run: |
          cd helm
          # Giả sử trong values-showtime.yaml bạn có cấu trúc:
          # image:
          #   repository: ten-dockerhub-cua-ban/showtime
          #   tag: "old-tag"
          
          # Dùng sed để replace dòng tag cũ thành tag mới (commit sha)
          sed -i 's/tag: .*/tag: "${{ steps.vars.outputs.sha_short }}"/g' values-showtime.yaml
          
          # Hiển thị ra để kiểm tra
          cat values-showtime.yaml | grep tag

      - name: Commit and push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add helm/values-showtime.yaml
          git commit -m "[Bot] Update showtime image tag to ${{ steps.vars.outputs.sha_short }}"
          git push origin main
